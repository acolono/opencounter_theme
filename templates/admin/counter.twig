{% extends "templates-site" %}

{% block content %}
    <h1>View Counter {{ name }}</h1>

<ul>
    <li class="counter__name">{{ name }}</li>
    <li class="counter__value">{{ value }}</li>
</ul>


{% endblock %}